name: WIF App registration with federation

permissions:
  id-token: write # This is required for requesting the JWT
  contents: read  # This is required for actions/checkout

on:
  push:
    branches:
    - main
env: 
  LOCATION: "northeurope"
  RESOURCE_GROUP_NAME: "WIF-AppReg-Federation"

jobs:
    job01:
        runs-on: ubuntu-latest
        environment: main
        steps:
        - uses: Azure/login@v2.0.0
          with:
            client-id: ${{ secrets.AZURE_CLIENT_ID }}
            tenant-id: ${{ secrets.AZURE_TENANT_ID }}
            subscription-id: "3978865e-6ad6-4904-afd3-384f6857e1bf"
        
        - name: Creat resource group
          uses: azure/CLI@v2.0.0
          with:
            inlineScript: |
                az graph query -q "where type == 'microsoft.resources/subscriptions/resourceGroups' | project name" -o tsv

